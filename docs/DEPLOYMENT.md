# Deployment

Not applicable yet — this fills in during Phase 9 with exact,
step-by-step Vercel and production-Supabase instructions (connecting the
repo, environment variables, auth redirect URLs, custom domain, launch
checklist), written for someone doing it for the first time.

Target hosting: **Vercel**. Target database/auth/storage: **Supabase**
(same project as development, or a separate production project — decided
when we get there).
